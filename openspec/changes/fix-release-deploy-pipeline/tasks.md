## 1. Unified workflow

- [x] 1.1 Rewrite `release.yml` — add build and deploy steps conditional on `release_created` output
- [x] 1.2 Delete `deploy.yml`

## 2. Verification

- [ ] 2.1 Push a fix/feat commit to master, merge release PR, verify app is deployed to GitHub Pages
