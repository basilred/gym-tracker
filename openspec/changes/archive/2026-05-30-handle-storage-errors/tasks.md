## 1. Обработка ошибок чтения

- [x] 1.1 Оборачиваем `JSON.parse` в try-catch в инициализаторе `useState`
- [x] 1.2 При ошибке: очищаем ключ в localStorage, возвращаем `[]`

## 2. Обработка ошибок записи

- [x] 2.1 Оборачиваем `localStorage.setItem` в try-catch внутри `useEffect`
- [x] 2.2 При ошибке: выводим `console.warn` с описанием

## 3. Проверка

- [x] 3.1 Запустить `npm run lint`, убедиться что ошибок нет
- [x] 3.2 Запустить `npm run build`, убедиться что сборка проходит
